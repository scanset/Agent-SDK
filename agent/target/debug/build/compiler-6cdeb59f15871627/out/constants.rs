
// Generated compile-time constants from TOML configuration
// Profile: development
// DO NOT EDIT - Generated by build.rs

pub mod compile_time {
    pub mod file_processing {
        pub const MAX_FILE_SIZE: u64 = 104857600;
        pub const LARGE_FILE_THRESHOLD: u64 = 5242880;
        pub const MAX_LINE_COUNT_FOR_ANALYSIS: usize = 500000;
        pub const PERFORMANCE_LOG_BUFFER_SIZE: usize = 5000;
    }

    pub mod lexical {
        pub const MAX_STRING_SIZE: usize = 2097152;
        pub const MAX_IDENTIFIER_LENGTH: usize = 512;
        pub const MAX_COMMENT_LENGTH: usize = 50000;
        pub const MAX_TOKEN_COUNT: usize = 5000000;
        pub const METRICS_BUFFER_SIZE: usize = 5000;
        pub const MAX_STRING_NESTING_DEPTH: u32 = 200;
    }

    pub mod syntax {
        pub const MAX_PARSE_DEPTH: usize = 200;
        pub const MAX_ERROR_HISTORY: usize = 100;
        pub const MAX_CONTEXT_STACK_DEPTH: usize = 50;
        pub const MAX_RECOVERY_SCAN_TOKENS: usize = 5000;
        pub const MAX_LOOKAHEAD_TOKENS: usize = 20;
    }

    pub mod symbols {
        pub const MAX_GLOBAL_SYMBOLS: usize = 100000;
        pub const MAX_LOCAL_SYMBOLS_PER_CTN: usize = 5000;
        pub const MAX_SYMBOL_RELATIONSHIPS: usize = 500000;
        pub const MAX_SYMBOL_IDENTIFIER_LENGTH: usize = 512;
        pub const MAX_SYMBOL_CONTEXT_DEPTH: usize = 100;
        pub const MAX_ELEMENTS_PER_SYMBOL: usize = 50000;
        pub const MAX_CTN_SCOPES: usize = 5000;
    }

    pub mod references {
        pub const MAX_REFERENCE_DEPTH: usize = 100;
        pub const MAX_REFERENCES_PER_SYMBOL: usize = 50000;
        pub const MAX_REPORTED_CYCLES: usize = 50;
        pub const MAX_CYCLE_LENGTH: usize = 200;
        pub const MAX_DEPENDENCY_NODES: usize = 500000;
        pub const MAX_RELATIONSHIPS_PER_PASS: usize = 5000000;
    }

    pub mod semantic {
        pub const MAX_SEMANTIC_ERRORS: usize = 5000;
        pub const MAX_RUNTIME_OPERATION_PARAMETERS: usize = 200;
        pub const MAX_SET_OPERATION_OPERANDS: usize = 5000;
        pub const MAX_ERROR_MESSAGE_LENGTH: usize = 50000;
        pub const MAX_CYCLE_PATH_LENGTH: usize = 200;
        pub const MAX_FILTER_STATE_REFERENCES: usize = 5000;
    }

    pub mod structural {
        pub const MAX_SYMBOLS_PER_DEFINITION: usize = 50000;
        pub const MAX_NESTING_DEPTH: usize = 20;
        pub const MAX_CRITERIA_BLOCKS: usize = 5000;
        pub const MAX_SET_OPERANDS: usize = 500;
        pub const MAX_VARIABLES_PER_DEFINITION: usize = 5000;
        pub const MAX_STATES_PER_DEFINITION: usize = 2500;
        pub const MAX_OBJECTS_PER_DEFINITION: usize = 1000;
    }

    pub mod batch_processing {
        pub const MAX_WORKER_THREADS: usize = 16;
        pub const MAX_FILES_PER_BATCH: usize = 5000;
        pub const MAX_BATCH_MEMORY: u64 = 2000000000;
    }

    pub mod security {
        pub const MEMORY_ALERT_THRESHOLD: u64 = 1000000000;
        pub const MAX_PROCESSING_TIME_SECONDS: u64 = 1800;
        pub const AUDIT_LOG_BUFFER_SIZE: usize = 50000;
        pub const MAX_CONCURRENT_OPERATIONS: usize = 200;
    }

    pub mod logging {
        pub const MAX_ERROR_COLLECTION: usize = 5000;
        pub const LOG_BUFFER_SIZE: usize = 50000;
        pub const MAX_LOG_MESSAGE_LENGTH: usize = 50000;
        pub const MAX_LOG_EVENTS_PER_FILE: usize = 5000;
        pub const MAX_CONCURRENT_LOG_OPERATIONS: usize = 200;
        pub const SECURITY_MIN_LOG_LEVEL: u8 = 1;
        pub const AUDIT_LOG_RETENTION_BUFFER: usize = 50000;
    }
}
